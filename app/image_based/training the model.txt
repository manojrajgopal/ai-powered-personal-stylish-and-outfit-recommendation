## Method 1When you run `app.py`, it generates the trained model files: `embeddings.pkl` and `filenames.pkl`.    📌 To run the script, navigate to the `image_based` directory:bash	cd image_based	python app.py## Method 2📌 Alternatively, you can use the pre-trained model files (which I have already trained).🔗 https://drive.google.com/drive/folders/1z2k4MkFjm7KrILWkZEvLzhOOebssninD?usp=sharingDownload the pre-trained model files and place them in the image_based directory: cd image_basedimage_based/            |───── embeddings.pkl            └──── filenames.pkl